# Data

This folder is for local datasets and intermediate processed files used while working through the book.

- `raw/` stores original, unmodified data.
- `processed/` stores any cleaned or transformed data derived from `raw/`.

Datasets are kept local and are ignored by Git by default.
